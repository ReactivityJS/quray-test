kernel:
  image: linuxkit/kernel:6.6.13
  cmdline: "console=ttyS0 quiet rootwait"

init:
  - local/crun:1.0      # Layer 1: crun-Binary (/usr/bin/crun)
  - local/system:1.0    # Layer 2: /init, /usr/local/bin/svc, /bundles/
